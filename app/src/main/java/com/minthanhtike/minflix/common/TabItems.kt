package com.minthanhtike.minflix.common

enum class TabItems(
    val label:String
) {
    Episodes(label = "Episodes"),
    Casts(label = "Casts"),
    Reviews(label = "Reviews"),
    Recommendation(label = "More like this")
}